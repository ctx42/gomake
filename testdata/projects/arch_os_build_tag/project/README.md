Project to test functions which list files based on values of GOOS and GOARCH
environment variables. Aside from GOOS and GOARCH all the files (except main.go,
main_helpers.go) are tagged with "//go:build gomake".