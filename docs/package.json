{
  "name": "gomake-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "hugo server --buildDrafts --disableFastRender --baseURL http://localhost:1313/",
    "build": "hugo --minify",
    "preview": "hugo server --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "@tailwindcss/typography": "^0.5.15",
    "tailwindcss": "^4.0.0"
  }
}
